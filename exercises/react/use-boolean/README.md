# useBoolean Hook

## Task
Create a custom React hook `useBoolean` that manages a boolean state.

### Requirements:
It should return an object or array with:
1. The current boolean `value`.
2. A `toggle` function.
3. A `setTrue` function.
4. A `setFalse` function.
