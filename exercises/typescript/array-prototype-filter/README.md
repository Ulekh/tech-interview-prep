# Array Prototype Filter

## Task
Implement `myFilter` on `Array.prototype`. It should behave exactly like the built-in `Array.prototype.filter()`.

### Requirements:
1. Do not use the built-in `.filter()` method.
2. It should handle sparse arrays correctly.
3. It should accept an optional `thisArg`.
